ui {
    env {
        destinationIcon {
            name = destination.name
            description = destination.description
        }

        categoryIcon {
            name = category.name
            description = category.name
        }
    }

    TODO("编写 UI 示例")
}